kindly follow below steps to run this app:

1. Open current folder 'inventory-manager' in command propmt then run below commands

2. inventory-manager>npm install 

3. inventory-manager> npm start